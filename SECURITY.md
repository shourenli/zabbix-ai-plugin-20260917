# 安全政策（Security Policy）

## 报告漏洞

**请不要用公开 issue 报告安全问题。**

- 首选（也是唯一渠道）：本仓库 **Security → Report a vulnerability**（GitHub 私密漏洞报告）

报告里请包含：受影响版本 / 复现步骤 / 影响面（能读到什么、能做什么）/ 你希望的处理方式。
我们会在 **3 个工作日内**确认收到，修复后同步进展；如需公开致谢请注明。

## 支持范围

| 版本 | 是否支持 |
|---|---|
| 最新 `v1.1.x` | ✅ 接受报告并修复 |
| 更早版本 | ❌ 请先升级到最新版（各版本修过什么问题见 [`CHANGELOG.md`](CHANGELOG.md)） |

> 说明：本仓库是自 `v1.1.0` 起**重建的干净仓库**，只包含 `v1.1.0` 及之后的标签；
> `v1.0.0` – `v1.0.9` 属于已删除的旧仓库（原因见文末「历史遗留」一节）。

## 这个项目是什么（信任边界）

- 一个**独立 Web 服务**（FastAPI，默认 `127.0.0.1:10801`），由 nginx 反代到与 Zabbix **同源**的 `/ai/`
- 一个**前端对话页**，以及一个**可选的漂浮球脚本**（`frontend/launcher.js`，由 nginx 注入到 Zabbix 页面）
- 它**不读 Zabbix 数据库**：所有监控数据都通过 Zabbix JSON-RPC API 获取，用的是**提问者自己的会话**

## 安全设计（有意为之的取舍，改动前请先读）

1. **大模型没有"发送"能力。** `send_dingtalk` 不在 LLM 工具表里，发送只能由用户在输入框以
   `/供应商名 消息内容` 触发，走确定性代码路径。原因：把有真实外部副作用的动作交给模型自主决定，
   实测出现过两类事故——**把与监控无关的问题转发到供应商群**，以及**没调工具却回答"已发送"**。
2. **鉴权完全复用 Zabbix 会话。** 读 `zbx_session` cookie → `user.get` 校验 → 用该用户的会话查数据
   （`auth.mode: user`）。因此**每个人的可见范围等同于他在 Zabbix 里的权限**；插件不建独立账号体系。
3. **密钥只走环境变量，且仓库与离线包都不带密钥。**
   - 仓库里的 `.env.example` 只放占位符；
   - **离线包里的 `.env.example` 由打包脚本现场生成**（常量内容、所有值为空），不再复制仓库文件——
     即使仓库文件将来被误填真实值，也不会随包分发（`deploy/package.py`）；
   - 打包前有**内容级密钥检查**：命中 LLM Key 形态、`sk-` 开头、钉钉 `access_token=<明文>`、私钥内容，
     或模板里出现非占位符值，都会**中止打包**。
4. **数据库账号最小权限、运行期零 DDL。** 应用账号按 `deploy/schema.sql` 只被授予
   `SELECT/INSERT/UPDATE/DELETE/CREATE/INDEX`；**任何迁移都不在运行期做**
   （历史教训：需要 `ALTER` 的迁移失败后被当成 warning 吞掉，结果是每次对话 502）。
5. **不修改 Zabbix 自身文件。** 漂浮球通过 nginx `sub_filter` 注入一行 `<script>`，
   因此 `apt upgrade zabbix-frontend-php` 不会覆盖它，卸载只需 `--uninstall`。
6. **仓库里不出现真实资产标识。** 文档、注释、测试数据统一使用合成值
   （主机名前缀 `AIQA_`/`DEMO_`、RFC 5737 文档保留地址、供应商名 `SUPPLIER_A`…），
   并有测试 `tests/test_docs.py::test_repo_contains_no_real_assets_or_secrets` 全仓扫描兜底：
   命中真实主机名形态、非保留网段 IP、密钥形态，或哈希命中禁止词表即测试失败。

## 已知限制（部署前请自行评估）

- **提问内容会发送给外部 LLM**（默认智谱 `glm-4-flash`）。如果问题里带主机名、IP、业务细节，
  这些信息会离开你的网络。介意的话把 `llm.base_url` 指向自建/内网模型。
- **对话历史**按"登录会话"隔离（键即 sessionid，复用 `sessions.user_id` 列存储）。
  能读到该表的人就能读到对话内容；表位于独立库、账号为最小权限。默认闲置 15 分钟自动清空，
  可通过 `history.ttl_minutes` 调整。
- **漂浮球的 iframe 与 Zabbix 同源，且刻意不加 `sandbox`**（否则无法携带 `zbx_session` 完成鉴权）。
  风险等级与"Zabbix 页面自身被 XSS"相同；请保持 Zabbix 前端与依赖的补丁更新。
- **未内置限流/防滥用**。请依赖 nginx 或网络层做速率限制。
- **服务账号模式（`auth.mode: service`）尚未贯通**：当前所有查询都使用登录用户自己的会话，
  `ZABBIX_SERVICE_*` 即使配置也不生效（详见 `README.md` 的「已知限制」）。

## ⚠️ 历史遗留：旧仓库的标签里曾包含一把真实密钥

本仓库的 git 历史**不含**任何真实凭据或真实资产标识：它自 `v1.1.0` 起重建，
`v1.1.0` 之前的 10 个标签（`v1.0.0` – `v1.0.9`）与对应的提交都不会出现在这里。

但**删除旧仓库并不能止损**——在那之前发布的 10 个标签的 `.env.example` 中含有一把
**真实可用的 LLM API Key**，并且被打进了 9 个离线包（每个包 24 个文件之一）。

> **如果你克隆、下载或分叉过那 10 个标签中的任意一个，请把该密钥视为已泄露，
> 到服务商处吊销并重新签发。** 只有轮换之后，散落在各处的副本才会失去价值；
> 删除文件、重写历史或删除仓库都无法收回已扩散的副本。

轮换后需要更新三处 `.env` 并重启插件：开发机仓库、测试机与生产机的
`/opt/zabbix-ai-plugin/.env`。
